# ============================================================
# 动作配置文件 — 由 YAML 编辑器生成
# 生成时间: 2026/6/10 14:13:17
# 来源: lingxing_elements_V10_词库管理页面_1_cleaned.json
# ============================================================

page_url: "https://ads.lingxing.com/amazon/word-stock"
close_browser: true
actions:

  - index: 0
    step_name: "搜索词库"
    action:
      type: check_visible
      on_not_found: skip
      wait_for_element: false

  - index: 1
    step_name: "搜索词库"
    action:
      type: fill
      on_not_found: fail
      wait_for_element: false
      value: "${INPUT_VALUE}"
      clear_first: true
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 2
    step_name: "搜索词库_父元素"
    action:
      type: check_visible
      on_not_found: skip
      wait_for_element: false

  - index: 3
    step_name: "搜索词库_父元素"
    action:
      type: check_visible
      on_not_found: skip
      wait_for_element: false

  - index: 4
    step_name: "搜索词库_关键词"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 5
    step_name: "搜索词库_关键词"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 6
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 7
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 8
    step_name: "搜索词库_新建分组"
    action:
      type: fill
      on_not_found: fail
      wait_for_element: false
      value: "${INPUT_VALUE_KEY}"
      clear_first: true
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 9
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 10
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 11
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 12
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 13
    step_name: "搜索词库_新建分组"
    action:
      type: fill
      on_not_found: fail
      wait_for_element: false
      value: "${INPUT_VALUE_KEY_VALUE1}"
      clear_first: true
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 14
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 15
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 16
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 17
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 18
    step_name: "搜索词库_新建分组"
    action:
      type: fill
      on_not_found: fail
      wait_for_element: false
      value: "${INPUT_VALUE_KEY_VALUE2}"
      clear_first: true
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout

  - index: 19
    step_name: "搜索词库_新建分组"
    action:
      type: click
      on_not_found: fail
      wait_for_element: false
      wait_before: 0.5
      wait_after: 0.5
      wait_strategy: timeout